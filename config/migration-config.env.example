# 🔄 Configuration pour la Migration PostgreSQL Azure → Hostinger
# Copiez ce fichier vers migration-config.env et configurez vos valeurs

# ===== AZURE POSTGRESQL (SOURCE) =====
AZURE_HOST=dev-dealtobook-postgres.postgres.database.azure.com
AZURE_PORT=5432
AZURE_USER=dealtobook
AZURE_PASSWORD=your_azure_postgres_password_here

# Bases de données à migrer (séparées par des espaces)
AZURE_DATABASES="deal_generator keycloak deal_setting"

# ===== HOSTINGER (DESTINATION) =====
HOSTINGER_IP=148.230.114.13
HOSTINGER_USER=root
HOSTINGER_POSTGRES_USER=dealtobook
HOSTINGER_POSTGRES_PASSWORD=devpassword123
HOSTINGER_POSTGRES_DB=dealtobook_db

# ===== OPTIONS DE MIGRATION =====
# Répertoire de backup (sera créé automatiquement)
BACKUP_DIR_PREFIX=postgres-migration

# Compression des backups (true/false)
COMPRESS_BACKUPS=true

# Nettoyage automatique des fichiers temporaires (true/false)
AUTO_CLEANUP=true

# Vérification post-migration (true/false)
VERIFY_MIGRATION=true

# ===== OPTIONS AVANCÉES =====
# Timeout pour les connexions (secondes)
CONNECTION_TIMEOUT=30

# Nombre de tentatives de connexion
MAX_RETRIES=3

# Options pg_dump supplémentaires
PG_DUMP_OPTIONS="--verbose --no-owner --no-privileges --no-tablespaces --clean --if-exists"

# Options psql supplémentaires
PSQL_OPTIONS="--quiet --single-transaction"

# ===== NOTIFICATIONS (OPTIONNEL) =====
# Slack webhook pour notifications
SLACK_WEBHOOK_URL=""

# Email pour notifications
NOTIFICATION_EMAIL=""

# ===== SÉCURITÉ =====
# Chiffrement des backups (true/false)
ENCRYPT_BACKUPS=false

# Clé de chiffrement GPG (si ENCRYPT_BACKUPS=true)
GPG_KEY_ID=""

# ===== MONITORING =====
# Activer les logs détaillés (true/false)
VERBOSE_LOGGING=true

# Conserver les logs (nombre de jours)
LOG_RETENTION_DAYS=30

# ===== EXEMPLE D'UTILISATION =====
# 1. Copiez ce fichier : cp migration-config.env.example migration-config.env
# 2. Configurez vos valeurs dans migration-config.env
# 3. Chargez la configuration : source migration-config.env
# 4. Lancez la migration : ./migrate-postgres-azure-to-hostinger.sh migrate
